{
  "name": "assignment2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts" : {   
    "watch": "babel src --presets react --out-dir public --watch --verbose",
    "compile": "babel src --presets react --out-dir public",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start" : "nodeman server/server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react": "^6.24.1"
  }
}
